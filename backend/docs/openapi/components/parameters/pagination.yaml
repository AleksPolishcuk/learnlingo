PageParam:
  in: query
  name: page
  schema:
    type: integer
    minimum: 1
    default: 1
  description: Page number (1-based)

LimitParam:
  in: query
  name: limit
  schema:
    type: integer
    minimum: 1
    maximum: 100
    default: 4
  description: Items per page
